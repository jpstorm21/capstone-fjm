scalar Date

type Query {
  getStates: [State]
}

type Mutation {
  createState(input: StateData): State
}

type State {
  id: ID!
  type: String!
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date!
}

input VenueData {
  name: String!
}
